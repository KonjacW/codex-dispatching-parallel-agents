---
name: dispatching-parallel-agents
description: Use when a task contains two or more potentially independent workstreams that may be handled concurrently without shared state, interfaces, or resources
---

# 并行代理调度

## 核心原则

主 agent 保留方向、共享接口、整合和最终验证；边界清晰且可独立验收的支线直接并行。并行的目的不是把工作全部外包，而是缩短独立工作流的总耗时。

## 快速判断

存在两个或以上分支，并且同时满足以下条件时，直接并行分派：

- 分支之间没有共享写入状态、接口决策或运行资源；
- 每个分支都有独立的目标和验收方式；
- 一个分支的结果不会改变其他分支的任务定义。

不要求主 agent 先完成主线，也不要求计算精确的成本收益。明显独立的调查、测试、资料整理、兼容性检查和不同模块的边界修改，默认适合并行。

## 保留主线

以下工作默认由主 agent 处理：

- 需求解释、总体架构、优先级和高风险决策；
- 共享接口、公共数据结构和跨模块调用链；
- 多个子结果的选择、合并、冲突处理和回归验证。

若多个失败点可能来自同一根因，先由主 agent 或一个子 agent 完成共享根因诊断；只有确认修复分支独立后，才并行实施。

任务很小且提示、等待、整合成本明显高于工作量时，主 agent 可直接完成。这是后置风险检查，不是普通并行的前置门槛。

## 有限递归

默认只由主 agent 向下分派一层。子 agent 只有在任务规模明显较大、新分支独立可验收、不争用文件或资源，并且它能负责本分支整合时，才可继续分派。

发现共享状态、目标改变、分支开始互相等待，或剩余工作主要是整合时，停止继续拆分并转为顺序处理。

## 最小提示契约

```text
范围：只处理……
目标：产出……
约束：不要修改/触碰……
验收：必须检查……
返回：说明发现、改动、验证结果和剩余风险。
```

提示只提供该分支所需的上下文，不重复完整项目背景。

## 整合与常见错误

- 子 agent 的报告不是完成证据；主 agent 必须检查实际 diff、产物或测试。
- 子 agent 失败时，主 agent 判断重试、接管、缩小范围或放弃，不因沉没成本盲目保留分支。
- 不要按文件数量机械拆分：同一根因、同一调用链或共享接口应保持在同一任务中。
- 不要为了“并行”分派微小任务，也不要让多个 agent 同时修改同一状态。

本 skill 只负责并行调度。完整项目计划、计划执行、工作树隔离、长期程序监控和完成前验证分别使用 `writing-plans`、`executing-plans`、`using-git-worktrees`、`long-running-progress-monitoring` 和 `verification-before-completion`。
